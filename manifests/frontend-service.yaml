apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: taskapp
  labels:
    app: taskapp-frontend
    environment: production
spec:
  type: ClusterIP
  selector:
    app: taskapp-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

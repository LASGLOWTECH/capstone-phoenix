apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: taskapp
  labels:
    app: taskapp-backend
    environment: production
spec:
  type: ClusterIP
  selector:
    app: taskapp-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP

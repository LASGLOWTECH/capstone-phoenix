# Manifests

This directory will host the Kubernetes manifests for the TaskApp platform and application.

Planned contents:
- namespace
- ConfigMap and Secret
- PostgreSQL StatefulSet + PVC
- backend and frontend Deployments
- ingress and TLS resources
- network policy, HPA, PDB, and probes
